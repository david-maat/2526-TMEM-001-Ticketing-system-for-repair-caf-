#BUILD-TIME VARIABLES
# Repository URL (for cloning the repo on first boot)
REPO_URL=https://github.com/Thomas-More-Digital-Innovation/2526-TMEM-001-Ticketing-system-for-repair-caf-.git

# Socket.IO Server URL
SOCKETIO_URL=https://localhost:3000

# Printer Name (unique identifier for this printer)
PRINTER_NAME=Printer-01

# Connection Type: 'network' or 'usb'
CONNECTION_TYPE=usb

# Network Printer Configuration (for CONNECTION_TYPE=network)
PRINTER_IP=192.168.1.8
PRINTER_PORT=9100

# USB Printer Configuration (for CONNECTION_TYPE=usb)
# On Windows, use the printer name as shown in Windows Settings > Printers & Scanners
# This is the RECOMMENDED method on Windows (easier and more reliable)
WINDOWS_PRINTER_NAME=POS-80C

# Alternative: Use USB VID/PID (requires libusb or pywin32)
# Only needed if WINDOWS_PRINTER_NAME is not set
USB_VENDOR_ID=0x0519
USB_PRODUCT_ID=0x0003
USB_INTERFACE=0
USB_IN_EP=0x81
USB_OUT_EP=0x03

# SSL Certificate Verification (set to false in development)
SSL_VERIFY=false

# Enable debug logging (set to true for troubleshooting)
DEBUG=false
